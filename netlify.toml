[build]
  command   = "echo skip"
  publish   = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA routing — all paths fall back to index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
